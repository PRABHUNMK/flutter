return Scaffold(
      appBar: AppBar(
        title: Text('computer science dept'),
      ),
      body: Center(
        child: TextButton(
          onPressed: () {},
          child: Text(
            'click me',
            style: TextStyle(
              color: Colors.black,
              backgroundColor: Colors.amber,
            ),
          ),
        ),
      ),
    );
